package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// High Density Theme Palette (Corporate Blue & Crisp Slate)
val HighDensityBlue = Color(0xFF2563EB)     // Corporate Blue 600
val HighDensityBlueDark = Color(0xFF1D4ED8) // Deep Tech Blue 700
val GlassBlue = Color(0xFFDBEAFE)           // Info pill glass background

val SchoolBusGold = HighDensityBlue         // Mapped to HighDensityBlue for flawless component compilation
val SchoolBusGoldDark = HighDensityBlueDark // Mapped to HighDensityBlueDark
val SlateDarkNavy = Color(0xFF0F172A)       // Crisp Dark Canvas base
val DeepCharcoal = Color(0xFF1E293B)        // Solid Dark Card surface
val LightSlateSurface = Color(0xFF334155)   // Slate outline details
val PureWhite = Color(0xFFFFFFFF)
val SubtleGray = Color(0xFF94A3B8)

val GreenOnline = Color(0xFF16A34A)         // Pure High-Density Status Green
val RedOffline = Color(0xFFEF4444)          // Pure High-Density Status Red
val StatusBlue = Color(0xFF3B82F6)          // Info Blue 500
